func main(): int {
    var i: int
    i=0
    repeat {
        if( true ){
            if( true ){
                if( true ){
                    continue
                }
            }
        }
        return 2
    } until(false)
}
